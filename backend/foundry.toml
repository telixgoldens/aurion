[profile.default]
src = "contracts"
test = "test"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
via_ir = true
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "write", path = "./deployments" }]


[fuzz]
runs = 256

[rpc_endpoints]
arbitrum = "${ARBITRUM_RPC_URL}"

